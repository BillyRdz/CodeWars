const instuctions = `Implement a function which convert the given boolean value into its string representation.

Note: Only valid inputs will be given.`;

let answer = `function booleanToString(b){
  return b.toString();
}`;

let arrowAnswer = `const booleanToString = b => b.toString();`;

console.log(instuctions);
console.log(answer);
console.log(arrowAnswer);
